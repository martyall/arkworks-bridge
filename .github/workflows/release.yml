name: tag-release

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: |
        cargo build --all --release
        tar -czf arkworks-bridge_linux_x86_64.tar.gz target/release/arkworks-bridge

    - name: Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          arkworks-bridge_linux_x86_64.tar.gz
